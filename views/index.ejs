<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>bloggy</title>
  <link href="https://cdn.jsdelivr.net/npm/remixicon@2.5.0/fonts/remixicon.css" rel="stylesheet">
  <link rel="stylesheet" href="../stylesheets/style.css">
</head>

<body>
  <% if(messages.info){ %>
    <div id="notify">
      <h4>Like Just Added !</h4>
    </div>
  <% } %>
  <div id="home">
<div id="nav">
<a href="/">blogg</a>
<div id="links">
  <a href="/blogs" me> All Blogs By User</a>
  <a href="/featured">(Updated) Featured Blogs</a>
</div>
<% if(loggedIn === true){ %>
  <a class="logOutBtn" href="/logout">Log Me Out</a> 
  <% } else{ %>
    <div id="btns">
      <a href="/register">Register</a>
      <a href="/login">Login</a>
    </div>
  <% } %>
</div>
<div id="content">
  <h1>Hey User ,  we've something really<br> good for you,<span id ="tdy">today .</span></h1>
  <a href="#">Explore More <i class="ri-arrow-right-line"></i></a>
  <div id="menu">
    <div id="extra">
     <div class="catg ">
      <i class="ri-heart-line"></i><h4>Liked Post</h4>
     </div>
     <div class="catg">
      <i class="ri-quill-pen-line"></i><h4>Recent Post</h4>
     </div>
     <div class="catg">
      <i class="ri-user-heart-fill"></i><h4>New User</h4>
     </div>
    </div>
    <div class="main">
      <% if(results !== undefined){ %>
      <% results.forEach(function(result){ %>
        <div class="blogexample">
          <div class="pic">
            <img src="<%= result.author.profileImage %>" alt="">
          </div>
          <div class="content">
            <h3><%= result.author.name%></h3>
            <h5><%=  %></h5>
            <p><%= result.post %></p>
            <div class="icons">
             <a href=<%= 'like/'+result._id  %>><i class="ri-thumb-up-line"></i></i></a> 
              <a href="#" ><i class="ri-thumb-down-line"></i></i></a>
            </div>
          </div>
        </div>
      <% }) %>
    <% } else { %>
      <a class="randomblog">No Post Available !</a>
    <% } %>
    </div>
  </div>
</div>
</div>
</body>
</html>